import 'package:flutter/material.dart';

class ServerSupportPage extends StatelessWidget {
  const ServerSupportPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('Server support and status information')),
    );
  }
}
